const User = require("../models/userModel")
const bcrypt = require("bcrypt")

exports.new = function(req, res) {
  console.log(req)
  /*var user = new User()

  user.name = "Igor Abreu"
  user.email = "igor.abreu.reis@gmail.com"
  user.password = 
  user.save(function(err) {
    res.json({
      message: "New contact created!",
      data: user
    })
  })*/
}
